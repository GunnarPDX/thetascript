[build-system]
requires = ["maturin>=1.5,<2"]
build-backend = "maturin"

[project]
name = "theta-script"
version = "2.4.0"
description = "open-financial-charts scripting language core (Rust) — Python bindings"
requires-python = ">=3.9"
license = "MIT"

[tool.maturin]
module-name = "theta_script"
